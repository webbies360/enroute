
$('.main-carousel').flickity({
    // options
    cellAlign: 'left',
    contain: true
});


$('.sliders').flickity({
    // options
    cellAlign: 'left',
    contain: true,
    prevNextButtons: false,
    
});
